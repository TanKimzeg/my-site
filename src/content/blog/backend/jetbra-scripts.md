---
title: ja-netfilter激活JB系列产品
description: 梳理ja-netfilter原理, 并提供一个用于生成激活密钥的工具。
pubDate: 2026 01 03 
categories: 
  - tech
tags:
  - java
  - crypto
image: /backend/idea-activated.png
---

在IDEA 2019的版本中，我们可以通过 IDE RESETTER 来反复试用30天。搜索这个插件，发现其作者的博客网站。浏览了其他文章，才听说了ja-netfilter这个工具。但是那里的[文章](https://zhile.io/2024/09/05/jetbrains-2024-2-region.html)语焉不详，我甚至不知道它是用来激活的。如果你直接在搜索引擎搜索IDEA破解，出来的都是各种网盘链接、或者关注公众号获取……非常恶心。其实这些jetbrains产品破解都来源于这个”插件“。所以，不用再去下载乱七八糟的百度网盘、关注什么乱七八糟的公众号，还有被植入恶意程序的风险。直接搜索ja-netfilter，就会出现很多真正的破解方法。例如[IDEA激活之ja-netfilter](https://blog.astorm.cn/archives/ideaji-huo-zhi-ja-netfilter)就是可靠的。

有了[热心大佬](https://3.jetbra.in/)提供的一键激活脚本（我看了一下脚本的内容，会将所有JB产品都注册一遍，在环境变量里面放💩，除非你自己懂得删减）。这些文章[^1]不使用VB脚本，触及了背后的原理，我对其产生了兴趣，并找到了一些讲原理的文章。

> 简单说是在 idea 的 jvm 配置上javaagent，执行某个 jar，然后对 idea 的授权流程做了一些 hook 操作。然后授权激活方式有两种，一种是通过激活码激活，另一种是通过授权服务器。[^1]

使用热心大佬的激活码会显示到2026-09-14过期，那么他的激活码和power.conf的参数是怎么来的呢？他没有说明，但被其他人发布了出来[^6]。我亲自操作，成功验证，可以自定义证书，包括你的名字、过期时间等。不知道是怎么回事，这些文章都是语焉不详，因此我写下原理，梳理流程，并将踩过的坑记录下来。

JB产品的激活码由三部分组成：licenseId - licensePartBase64 - signature - certBase64。加密算法用的是4096位RSA。我们都知道RSA签名的验证原理：密文经过哈希函数得到定长摘要，摘要用私钥签名（即进行幂模运算）。验证的时候，用公钥进行幂模运算相当于逆运算，得到解密后的摘要。在这里，产品是这样验证的：从certBase64可得到公钥，对sigature幂模运算得到licencePart的摘要，与licensePartBase64解码后进行比较，如果相同，说明激活有效。虽然cert不由官方私钥生成，本应无法经过幂模运算中的参数（即产品内置公钥）得到正确的licensePartBase64的摘要。但借助JVM技术，幂模函数使用的是[java.math.BigInteger](https://tankimzeg.top/blog/backend/biginteger-src/)类的modPower函数，对幂模函数注入（power.config），JVM提前返回我们想要的结果（licesePartBase64用我们自己生成的私钥进行签名的结果），软件从而被破解。

这样，我们就不难理解ja-netfilter的原理了。最关键的 `power.config` 配置了注入的结果。

```txt
[Result]
EQUAL,signature_value,65537,860106576...->fake_result
```

在这里，`<65537,860106576...>` 这两个数就是JB的公钥。ja-netfilter通过对modPower函数进行修改，使得
`signature_val.modPower(65537,860106576...)` 等于 `fake_result` 。

> [!NOTE]
> 一定要注意，严格按照 `EQUAL,signature_value,65537,860106576...->fake_result` 的格式，中间不能有空格、换行。由于数字很长，我一开始把它分隔成几行，导致总是激活不成功！

有一种激活方法是设置证书服务器为热佬的网站，其实也是热佬网站那边私钥替换，原理相似。

至于其他配置，如 `dns.config`, `url.config` 则是对特定的网络请求提前返回不可达的请求错误，阻止本地产品联网与JB官方服务器校验证书的有效性。这倒不是一定要在这里配置，我们也可以通过防火墙等方式拦截相关请求。这里面的网址，是通过抓包就能发现。

原理理解起来不难，难的是从无到有捕捉蛛丝马迹，结合JVM原理，JVM汇编[^2]，RSA密码学[^3]，网络流量分析[^4]，接口逆向[^5]等专业知识实现逆向破解。佩服作者，收获颇丰！

经过我的测试，此方法在目前最新的2025.3.1系列产品均可用。为了方便大家生成自己的激活码，我编写了一些python程序，对生成激活码、power.config配置的整个流程进行了一些封装，项目代码已开源在 [https://github.com/TanKimzeg/jetbra-scripts](https://github.com/TanKimzeg/jetbra-scripts)

---

> 虽然我不太喜欢Java语言，它有不少缺陷，但JVM真的是一个很厉害的技术！同时我在想，javaagent会不会使java程序变得不安全，能够被恶意注入呢？

## 参考资料

1. [IDEA激活之ja-netfilter](https://blog.astorm.cn/archives/ideaji-huo-zhi-ja-netfilter)
2. [ja-netfilter power插件原理](https://www.xuzhengtong.com/2022/07/25/ja-netfilter/ja-netfilter-plugins-power/)
3. [RSA的各种格式](https://blog.csdn.net/weixin_42744482/article/details/146378089)
4. [分析ja-netfilter如何破解jetbrains的IDE](https://zhuanlan.zhihu.com/p/494706735)
5. [破解软件的技术原理是什么？](https://www.zhihu.com/question/1976611088280883998/answer/1990376233867969393)

[^1]: [IDEA激活之ja-netfilter](https://blog.astorm.cn/archives/ideaji-huo-zhi-ja-netfilter)

[^2]:[分析ja-netfilter如何破解jetbrains的IDE](https://zhuanlan.zhihu.com/p/494706735)

[^3]:[RSA的各种格式](https://blog.csdn.net/weixin_42744482/article/details/146378089)

[^4]: [JetBrains新版本区域选择的坑](https://zhile.io/2024/09/05/jetbrains-2024-2-region.html)

[^5]: 插件代码仓库: [https://gitee.com/ja-netfilter/ja-netfilter](https://gitee.com/ja-netfilter/ja-netfilter)

[^6]: [ja-netfilter power插件原理](https://www.xuzhengtong.com/2022/07/25/ja-netfilter/ja-netfilter-plugins-power/)
